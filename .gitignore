# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
.astro
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# LaTeX build artifacts. cv/ holds only the source (cv.tex); the PDF that the
# site links to lives in public/, so the repo never carries two copies that
# can drift apart.
cv/*.pdf
cv/*.aux
cv/*.out
cv/*.toc
cv/*.fls
cv/*.fdb_latexmk
cv/*.synctex.gz
